--- src/cli.ts
+++ src/cli.ts
@@ -62,6 +62,10 @@
     }

     switch (token) {
+      case "--json":
+      case "-j":
+        options.json = true;
+        continue;
       case "-h":
       case "--help":
         options.help = true;
-      case "-j":
-      case "--json": {
-        options.json = true;
-        const next = argv[i + 1];
-        if (
-          next !== undefined
-          && !next.startsWith("-")
-          && queryFromFlag === undefined
-          && !isCommandToken(next)
-        ) {
-          queryFromFlag = next;
-          i += 1;
-        }
-        continue;
-      }
