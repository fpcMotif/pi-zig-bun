name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      CACHE_VERSION: v2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache Bun globals and dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: bun-${{ runner.os }}-${{ env.CACHE_VERSION }}-${{ hashFiles('bun.lock') }}-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ env.CACHE_VERSION }}-

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v3
        with:
          version: 0.15.2

      - name: Cache Zig build artifacts
        uses: actions/cache@v4
        with:
          path: |
            .zig-cache
            ~/.cache/zig
          key: zig-${{ runner.os }}-${{ env.CACHE_VERSION }}-0.15.2-${{ hashFiles('build.zig') }}-${{ hashFiles('src/zig/main.zig') }}-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            zig-${{ runner.os }}-${{ env.CACHE_VERSION }}-0.15.2-

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry and binaries
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
          key: cargo-${{ runner.os }}-${{ env.CACHE_VERSION }}-stable-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            cargo-${{ runner.os }}-${{ env.CACHE_VERSION }}-

      - name: Install dependencies
        run: bun install

      - name: Validate TypeScript
        run: bun run validate

      - name: Run unit and integration tests
        run: bun run test

      - name: Build Zig backend
        run: zig build

      - name: Install or ensure prek
        run: |
          if ! command -v prek >/dev/null 2>&1; then
            cargo install prek
          else
            echo "prek already available"
          fi

      - name: Run prek pre-commit checks
        run: prek run

      - name: Run search regression checks
        run: bun run ci:search-regression

      - name: Check requirements trace coverage
        run: bun run ci:requirements-coverage
