# Requirements Acceptance Matrix

This matrix traces requirements from `spec.md`, `prd.json`, and `user-stories.md` into objective acceptance criteria (验收标准), implementation mapping, validation commands, and current status.

## Status legend
- **done**: implemented and currently validated by CI commands.
- **partial**: partially implemented and/or only partially validated.
- **missing**: not implemented yet.

## Matrix

| Requirement ID | Source | Requirement | Measurable acceptance criteria (验收标准) | Implementation location(s) | Test case ID(s) | CI command(s) | Status |
|---|---|---|---|---|---|---|---|
| REQ-ARCH-001 | spec §1, prd goals | Mixed runtime split: Zig for search/index/TUI and Bun for agent/tooling. | Build outputs include Zig backend (`zig build`) and Bun CLI entrypoint (`bun run index.ts --help`) with no runtime error. | `src/zig/main.zig`, `src/main.ts`, `build.zig`, `package.json` | TC-ARCH-001 | `zig build && bun run validate` | partial |
| REQ-ARCH-002 | spec §3, user story A1 | Bun↔Zig JSON-RPC 2.0 over stdin/stdout. | JSON-RPC requests/responses serialize with `jsonrpc: "2.0"`; `search.files` and `search.grep` roundtrip successfully. | `src/search/bridge.ts`, `src/search/client.ts`, `src/zig/main.zig` | TC-RPC-001, TC-RPC-002 | `bun run ci:search-regression` | done |
| REQ-ARCH-003 | user story A1 | Bun→Zig→Bun query latency target. | p95 Bun-requested `search.files` roundtrip < **15ms** for warmed 50k index. | `src/search/bridge.ts`, `src/zig/main.zig` | TC-PERF-001 | `bun run ci:perf-bridge` (to add) | missing |
| REQ-ARCH-004 | spec §3 | UI bridge methods available. | JSON-RPC methods `ui.update(data)` and `ui.input(event)` accepted with typed payloads and no protocol errors. | `src/zig/main.zig`, `src/main.ts` (planned UI bridge module) | TC-RPC-UI-001 | `bun run ci:rpc-ui` (to add) | missing |
| REQ-SEARCH-001 | spec §2.1, user story B1 | Persistent binary index cache. | Search index persisted at `~/.pi/cache/search/<hash>/index.bin`; restart reuses cache without full reindex. | `src/zig/main.zig` (planned persistence layer) | TC-SEARCH-001 | `bun run ci:index-cache` (to add) | missing |
| REQ-SEARCH-002 | spec §4, prd perf target | Warmed search latency target. | `search.files` p95 latency < **10ms** on 50k-file benchmark corpus. | `src/zig/main.zig`, `scripts/ci-search-regression.ts` | TC-PERF-002 | `bun run ci:search-regression && bun run ci:perf-search` (to add) | partial |
| REQ-SEARCH-003 | spec §4, prd perf target, user story B1 | Initial indexing speed target. | First index of 50k files completes in < **2.0s** on benchmark environment. | `src/zig/main.zig` | TC-PERF-003 | `bun run ci:perf-index` (to add) | missing |
| REQ-SEARCH-004 | spec §4, user story B1 | Incremental index updates via file watching. | File add/remove/modify reflected in search results within **1s** without full reindex. | `src/zig/main.zig` (planned watch subsystem) | TC-SEARCH-004 | `bun run ci:index-watch` (to add) | missing |
| REQ-SEARCH-005 | spec §4, user story B1 | Ignore rule support. | `.gitignore`, `.ignore`, and `.geminiignore` all parsed; ignored files never returned in `search.files`/`search.grep`. | `src/zig/main.zig` | TC-SEARCH-005 | `bun run ci:search-regression` | partial |
| REQ-SEARCH-006 | spec §2.1, user story B2 | Typo-resistant fuzzy matcher. | Levenshtein/edit-distance ranking returns intended file in top-5 for 1–2 typo queries in fixture corpus. | `src/zig/main.zig`, `scripts/ci-search-regression.ts` | TC-SEARCH-006 | `bun run ci:search-regression` | done |
| REQ-SEARCH-007 | spec §2.1, spec §4, prd should-have, user story B3 | Ranking signals and weights. | Final score formula includes `fuzzy_score` + `git_bonus` (+20%) + `frecency_bonus` (+15%) + `proximity_bonus` (+10%), with deterministic order for ties. | `src/zig/main.zig` | TC-RANK-001 | `bun run ci:ranking` (to add) | partial |
| REQ-TUI-001 | spec §2.1, prd perf target | Zig-native TUI render performance. | Median render frame time ≤ **16ms** (60fps equivalent) under streaming output workload. | `src/zig/tui/*` (planned), `src/zig/main.zig` | TC-TUI-001 | `bun run ci:tui-perf` (to add) | missing |
| REQ-AGENT-001 | spec §2.2, user story A2, prd must-have | Bun-native agent orchestrator. | Main chat/orchestration loop implemented in TypeScript/Bun with prompt assembly and tool dispatch. | `src/main.ts`, `src/tools/*` | TC-AGENT-001 | `bun run validate` | partial |
| REQ-AGENT-002 | spec §2.2, user story A2, prd must-have | Streaming provider responses via Bun fetch. | SSE streaming supported for OpenAI + Anthropic; first token appears within **2s** for smoke prompt. | `src/providers/*` (planned), `src/main.ts` | TC-AGENT-002 | `bun run ci:provider-streaming` (to add) | missing |
| REQ-AGENT-003 | prd must-have | Multi-provider support. | OpenAI, Anthropic, Google GenAI provider adapters pass schema and error handling contract tests. | `src/providers/*` (planned) | TC-AGENT-003 | `bun run ci:providers` (to add) | missing |
| REQ-SESSION-001 | spec §2.2, prd must-have | JSONL session tree format. | Session records contain `id`/`parentId`; history replay reconstructs branch exactly; file is newline-delimited JSON objects. | `src/session/tree.ts` | TC-SESSION-001 | `bun run ci:search-regression` | done |
| REQ-SEC-001 | spec §5, prd security principles | Deny-by-default capability system. | If capability is absent from policy, operation is blocked and returns deterministic denial error. | `src/permissions.ts` | TC-SEC-001 | `bun run validate` | done |
| REQ-SEC-002 | spec §5, prd must-have, user story C2 | Capability gating by scope. | `allow_fs_read`, `allow_fs_write`, `allow_exec`, `allow_net` each enforce allowlist/regex semantics against attempted operation. | `src/permissions.ts`, `src/main.ts` | TC-SEC-002 | `bun run validate` | partial |
| REQ-SEC-003 | prd security | Audited execution logging. | Every deny/allow decision logs timestamp, actor, capability, target, and decision to append-only audit sink. | `src/permissions.ts` (planned audit logger) | TC-SEC-003 | `bun run ci:security-audit` (to add) | missing |
| REQ-TOOLS-001 | spec §2.2, prd must-have | Default toolset semantics. | `read`, `write`, `edit`, `bash` tools execute with documented arguments, deterministic JSON result envelope, and non-zero exit handling. | `src/tools/builtin.ts`, `src/tools/types.ts` | TC-TOOLS-001 | `bun run ci:search-regression` | done |
| REQ-EXT-001 | spec §5, prd must-have, user story C1 | TypeScript skills/extensions. | `.ts` skill modules load, can register tools, and execute using Bun runtime APIs. | `src/extensions/loader.ts`, `src/extensions/types.ts` | TC-EXT-001 | `bun run validate` | done |
| REQ-EXT-002 | user story C1 | Skill hot-reload behavior. | Modified skill file reloads within **2s** and new behavior is applied without process restart. | `src/extensions/loader.ts` (watch support planned) | TC-EXT-002 | `bun run ci:skill-hotreload` (to add) | missing |
| REQ-CLI-001 | spec §6 | `pi` interactive mode. | Running `pi` starts prompt and accepts `/help`, `/tree`, `/search`, `/grep`, `/quit` command semantics with exit code 0 on graceful quit. | `src/main.ts`, `src/cli.ts` | TC-CLI-001 | `bun run validate` | partial |
| REQ-CLI-002 | spec §6 | `pi -p "query"` one-shot print mode. | `-p` flag accepted; prints answer/search output without interactive prompt; exit code 0 for success. | `src/cli.ts`, `src/main.ts` | TC-CLI-002 | `bun run ci:cli-flags` (to add) | missing |
| REQ-CLI-003 | spec §6 | `pi --json "query"` JSON mode. | `--json` one-shot output is valid JSON object/array and contains stable keys for consumers. | `src/cli.ts`, `src/main.ts` | TC-CLI-003 | `bun run ci:search-regression` | partial |
| REQ-CLI-004 | spec §6 | `/login` auth setup command. | `/login` command persists provider auth config and reports masked key metadata only (no secret echo). | `src/main.ts` (planned auth module) | TC-CLI-004 | `bun run ci:auth-login` (to add) | missing |
| REQ-CLI-005 | spec §6 | `/tree` session history command. | `/tree` lists active heads/branches; JSON mode returns parseable array of session nodes. | `src/main.ts`, `src/session/tree.ts` | TC-CLI-005 | `bun run ci:search-regression` | done |
| REQ-CLI-006 | spec §6 | `pi search <query>` wrapper command. | Returns ranked file results; zero matches handled gracefully; exit code 0 unless internal error. | `src/main.ts`, `src/search/client.ts` | TC-CLI-006 | `bun run ci:search-regression` | done |
| REQ-PRD-001 | prd should-have | Web-view mode via Bun HTTP server. | `pi web` starts local HTTP server, renders session+search results, and shuts down cleanly on SIGINT. | `src/web/*` (planned) | TC-WEB-001 | `bun run ci:web-mode` (to add) | missing |
| REQ-PERF-004 | prd perf target | Cold start target. | Startup to interactive prompt < **150ms** on baseline runner with warmed deps/cache. | `src/main.ts`, `src/search/bridge.ts` | TC-PERF-004 | `bun run ci:perf-cold-start` (to add) | missing |
| REQ-MILE-001 | prd milestones M1 | M1 skeleton deliverables. | Zig search binary + Bun harness + JSON-RPC bridge all present and build in CI. | `src/zig/main.zig`, `src/main.ts`, `src/search/*` | TC-MILE-001 | `zig build && bun run validate && bun run ci:search-regression` | done |
| REQ-MILE-002 | prd milestones M2 | M2 search deliverables. | Indexer + fuzzy matcher + grep all functional and regression tested. | `src/zig/main.zig`, `scripts/ci-search-regression.ts` | TC-MILE-002 | `bun run ci:search-regression` | done |
| REQ-MILE-003 | prd milestones M3 | M3 agent MVP deliverables. | Streaming chat + tool execution + JSONL sessions complete in same interactive runtime. | `src/main.ts`, `src/tools/*`, `src/session/tree.ts` | TC-MILE-003 | `bun run ci:agent-mvp` (to add) | partial |
| REQ-MILE-004 | prd milestones M4 | M4 extensions deliverables. | TS skill loader + capability gating + npm-compatible execution all validated. | `src/extensions/*`, `src/permissions.ts` | TC-MILE-004 | `bun run validate && bun run ci:security-audit` (to add) | partial |
| US-P1-001 | user story P1 | Solo dev fast start + instant fuzzy search + responsive streaming chat. | Prompt ready within **150ms**, fuzzy search responds < **10ms p95**, streaming token cadence ≥ **5 tokens/s** after first token. | `src/main.ts`, `src/search/*`, `src/zig/main.zig` | TC-US-P1-001 | `bun run ci:perf-cold-start && bun run ci:perf-search && bun run ci:provider-streaming` (to add) | partial |
| US-P2-001 | user story P2 | Power users can author TS extensions with Bun/Node APIs. | Skills can import npm modules, call fetch/fs, and register tools without core recompilation. | `src/extensions/loader.ts`, `src/extensions/types.ts` | TC-US-P2-001 | `bun run validate` | done |
| US-P3-001 | user story P3 | Team-shared repo-scoped config via `.pi/`. | `.pi/` local policy + skills are loaded deterministically; lockfile-pinned dependencies respected in CI. | `src/main.ts`, `src/extensions/loader.ts`, `.github/workflows/ci.yml` | TC-US-P3-001 | `bun install --frozen-lockfile && bun run validate` | partial |
| US-P4-001 | user story P4 | Integrator-friendly SDK/RPC mode with stable schema + deterministic streams. | RPC schemas versioned; event ordering deterministic under replay for same seed/session input. | `src/search/bridge.ts`, `src/search/types.ts` | TC-US-P4-001 | `bun run ci:rpc-contract` (to add) | partial |

## Notes on CI coverage

- Existing CI validates TypeScript typing, Zig build, pre-commit policy, and search regression smoke tests.
- Rows marked `missing`/`partial` with `(...to add)` indicate required CI jobs for full objective verification and should be implemented before claiming requirement closure.
