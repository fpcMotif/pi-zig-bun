{
  "name": "pi-zig-bun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "precommit": "./scripts/pre-commit.sh",
    "validate": "bunx tsc --noEmit",
    "ci:search-regression": "bun scripts/ci-search-regression.ts",
    "ci": "bun run validate && zig build && bun run ci:search-regression && bun run ci:bench",
    "bench:fixture": "bun benchmarks/generate-fixture.ts",
    "bench": "bun benchmarks/run.ts",
    "ci:bench": "bun scripts/ci-bench-thresholds.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
