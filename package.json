{
  "name": "pi-zig-bun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "precommit": "./scripts/pre-commit.sh",
    "validate": "bun x tsc --noEmit",
    "test": "bun test",
    "test:zig": "zig test src/zig/main.zig",
    "ci:search-regression": "bun scripts/ci-search-regression.ts",
    "ci:perf": "bun scripts/perf-gates.ts",
    "ci:requirements-coverage": "bun scripts/check-requirements-coverage.ts",
    "ci": "bun run validate && zig build && bun run test && bun run test:zig && bun run ci:search-regression && bun run ci:perf && bun run ci:requirements-coverage"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
